import { PropertyFeatureEntity } from './property-feature.entity';

describe('PropertyFeatureEntity', () => {
  it('should be defined', () => {
    expect(new PropertyFeatureEntity()).toBeDefined();
  });
});
